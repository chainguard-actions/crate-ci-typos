name: Test custom config input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          files: actions/hardened/crate-ci--typos/v1.46.3/tests/fixtures/with-custom-word.txt
          config: actions/hardened/crate-ci--typos/v1.46.3/tests/fixtures/typos-config.toml
      - name: Assert action completed without error
        run: |
          echo "PASS: typos action ran successfully with custom config"
